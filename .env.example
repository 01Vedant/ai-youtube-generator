# Maintenance Mode
MAINTENANCE_MODE=0                # 1 to enable maintenance (returns 503 JSON)
MAINTENANCE_UNTIL=                # ISO8601 timestamp (optional); sets Retry-After and countdown
MAINTENANCE_MESSAGE=We’re doing scheduled maintenance.  # Message shown to users
MAINTENANCE_ALLOW_ADMINS=1        # Allow admin users to bypass when 1
MAINTENANCE_ALLOWLIST_IPS=        # Comma-separated client IPs allowed during maintenance
# ============================================================================
# AUTH & TENANCY (NEW)
# ============================================================================
JWT_SECRET=change-me-to-strong-random-secret-in-production
JWT_EXPIRE_HOURS=24
MAGIC_LINK_TTL_MINUTES=15
MAGIC_LINK_FROM=noreply@devotionalai.example.com
SECURE_COOKIES=true

# ============================================================================
# STRIPE BILLING (NEW)
# ============================================================================
STRIPE_API_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_ENTERPRISE=price_...

# ============================================================================
# USAGE QUOTAS (NEW - Free tier defaults)
# ============================================================================
QUOTA_IMAGES_COUNT=500
QUOTA_TTS_SECONDS=60000
QUOTA_RENDER_MINUTES=500
QUOTA_STORAGE_MB=100000
QUOTA_UPLOADS_COUNT=100
USAGE_LOG_DIR=./platform/usage

# ============================================================================
# BACKUPS & RECOVERY (NEW)
# ============================================================================
BACKUP_DIR=./platform/backups
BACKUPS_CRON=0 2 * * *
BACKUPS_RETENTION_DAYS=90
TENANT_DEFAULT_PLAN=free

# ============================================================================
# FAST PATH GPU RENDERING (NEW)
# ============================================================================
FAST_PATH=1
ENCODER=h264_nvenc
TARGET_RES=1080p
RENDER_MODE=FINAL
CQ=18
VBR_TARGET=20M
MAXRATE=40M
BUFSIZE=80M
FPS=30
MUSIC_DB=-12
WATERMARK_POS=br
UPSCALE=none
TMP_WORKDIR=/tmp/bhakti

# ============================================================================
# TEXT-TO-SPEECH (TTS) - Hindi Narration
# ============================================================================
TTS_PROVIDER=edge                   # Options: edge (Microsoft Edge TTS - production default), mock
TTS_VOICE=hi-IN-SwaraNeural        # Default Hindi voice (options: hi-IN-SwaraNeural [Calm], hi-IN-DiyaNeural [Narration])
TTS_RATE=-10%                       # Rate adjustment (negative = slower, soothing)
TTS_STRICT=False                    # Fail render on TTS errors (True) or fallback to mock with silent WAV (False)
AUDIO_PACE_TOLERANCE=0.02           # 2% tolerance for duration matching (±2% tightened from ±5%)
AUDIO_SAMPLE_RATE=24000             # Sample rate in Hz (24kHz)
TTS_PAUSE_MS=200                    # Pause between clauses (ms)

# ============================================================================
# EXISTING CONFIG (PRESERVED)
# ============================================================================
PORT=8000
BACKEND_PORT=8000
FRONTEND_PORT=80

STORAGE_PROVIDER=s3
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=bhakti
S3_SECRET_KEY=bhakti123
S3_BUCKET=bhakti-assets

MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

OPENAI_API_KEY=
ELEVENLABS_API_KEY=
ENABLE_YOUTUBE_UPLOAD=0

SENTRY_DSN=
RATE_LIMIT_PER_MIN=10

MAX_SCENES=20
MAX_SCENE_DURATION_SEC=20
MAX_TOTAL_DURATION_SEC=600
MAX_JOB_COST=50.0

API_KEY_ADMIN=
API_KEY_CREATOR=

REDIS_URL=
VITE_SENTRY_DSN=

FRONTEND_PUBLIC_URL=http://localhost:80
BACKEND_PUBLIC_URL=http://localhost:8000
