APP_ENV=prod
APP_BASE_URL=http://127.0.0.1:8000
CORS_ALLOWED_ORIGINS=http://localhost:5173
CSRF_SECURE_COOKIES=true# S3/MinIO (optional)
STORAGE_ENDPOINT=http://127.0.0.1:9000
STORAGE_BUCKET=bhaktigen
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin
STORAGE_REGION=us-east-1
STORAGE_SECURE=false

# Stripe (billing)
STRIPE_SECRET=sk_test_xxx
STRIPE_PRICE_PRO=price_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
APP_BASE_URL=http://localhost:5173

# Observability (optional)
SENTRY_DSN=

# Retention / Purge
RETENTION_DAYS=30               # delete artifacts older than N days
RETENTION_MIN_JOBS=50           # never purge if total completed jobs < this
RETENTION_DRY_RUN=0             # when 1, log but do not delete
RETENTION_SAFETY_HOURS=2        # grace hours after completion before eligible
PURGE_CRON=daily                # used by docs/scripts; worker supports on-demand

# Security
ACCESS_EXPIRES_MIN=15           # access token lifetime (minutes)
REFRESH_EXPIRES_DAYS=14         # refresh token lifetime (days)
APP_ENV=dev                     # 'prod' enables Secure cookies and strict CORS
CORS_ALLOWED_ORIGINS=            # comma-separated origins used when APP_ENV=prod
JWT_SECRET=dev-secret           # HS256 secret for tokens
