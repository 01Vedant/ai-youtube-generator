import { PublicShell } from '@/components/PublicShell';

export default function NotFoundPage() {
  return (
    <PublicShell>
      <div style={{ padding: 24, textAlign: 'center' }}>
        <h2>Page not found</h2>
        <p>Try the home page or templates.</p>
      </div>
    </PublicShell>
  );
}
